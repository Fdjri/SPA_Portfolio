export interface ProjectData {
  id: string;
  title: string;
  category?: string;
  date?: string;
  author?: string;
  description: string;
  features: string[];
  framework: string;
  images: string[];
}
